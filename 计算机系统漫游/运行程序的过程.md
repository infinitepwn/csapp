## shell
shell 是一个普通的程序，它接受用户输入的命令并且执行它们，它也是传统 Unix 系统中最基本的用户界面。shell 作为一个普通程序，而不是内核的一部分，充分说明了系统调用接口的强大：shell 并不是一个特别的用户程序。这也意味着 shell 是很容易被替代的，实际上这导致了现代 Unix 系统有着各种各样的 shell，每一个都有着自己的用户界面和脚本特性。

## shell输入指令
在shell中输入./hello时，键盘通过USB控制器，把字符读入(控制器的寄存器？)，然后通过总线输送到内存中
![image.png|503](https://raw.githubusercontent.com/infinitepwn/note_picbed/main/20260619152138288.png)

## shell执行指令加载hello
前面把./hello输送到内存中了，然后这样shell从内存中读取到这条命令，输入回车之后，shell就会执行指令来加载可执行的hello文件，这些指令将hello文件中的代码和数据从磁盘复制到主存
![image.png|500](https://raw.githubusercontent.com/infinitepwn/note_picbed/main/20260619151319453.png)

## 处理器执行机器语言
当hello被加载到主存中时，CPU开始执行hello程序，将hello，world复制到寄存器，寄存器通过总线再复制到显示设备，最终显示在屏幕上